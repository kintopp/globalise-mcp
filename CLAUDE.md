# CLAUDE.md

## Repository Overview

GLOBALISE MCP Server for accessing Dutch East India Company (VOC) transcriptions. Main code in `globalise-mcp-server/`. API docs in `globalise-transcriptions-api/`.

## Commands

All commands run from `globalise-mcp-server/`:

```bash
npm run build      # 3 stages: vite UI build → tsc → ensure:db (decompress committed DB)
npm run build:db   # regenerate data/archival-index.sqlite from source (slow; build uses ensure:db)
npm run dev        # tsc --watch (npm run dev:ui rebuilds the viewer on change)
npm run inspector  # build + MCP Inspector against dist/index.js
npm start          # run the built server (node dist/index.js)
npm test           # archival-index + viewer-build + smoke tests
npm run build:mcpb # pack the full .mcpb (ships the ~112MB index); :thin omits it (downloads on first use)
npm run test:mcpb  # stdio smoke-test the staged bundle (NOT in `npm test` — needs build:mcpb first)
```

## Structure (globalise-mcp-server/)

- `src/index.ts` — entry point, tool registration
- `src/tools/` — search, document, document-viewer, convenience, archival-index
- `src/transports/http-server.ts` — HTTP transport (Railway deployment)
- `src/utils/` — api-client, cache, database (SQLite), document-id, iiif, languages, errors, origin (Origin-header validation, spec MUST), types
- `apps/document-viewer/` — MCP Apps UI, bundled by vite during build
- `scripts/build-archival-db.ts` — regenerates `data/archival-index.sqlite` (committed as `.gz`)
- `skills/globalise-voc-research/` — the `globalise-voc-research` skill: `SKILL.md` (the source) + `globalise-voc-research.skill` (the committed package).
- `manifest.json` / `manifest.thin.json`, `.mcpbignore`, `MCPB.md`, `scripts/build-mcpb.ts`, `scripts/test-mcpb-bundle.ts` — the `.mcpb` (Claude Desktop bundle) packaging (since v2.9.0, on `main`). `mcpb-build/` (generated output) is gitignored.

### Rebuilding the `.skill` package

The `.skill` is just a zip containing `SKILL.md` under its directory prefix (no build script). After editing `SKILL.md`, repackage it from `globalise-mcp-server/skills/` and commit both files:

```bash
cd globalise-mcp-server/skills
rm -f globalise-voc-research/globalise-voc-research.skill
zip -X globalise-voc-research/globalise-voc-research.skill globalise-voc-research/SKILL.md
# verify: unzip -p .../globalise-voc-research.skill 'globalise-voc-research/SKILL.md' | diff - globalise-voc-research/SKILL.md
```

### Building the `.mcpb` bundles

`npm run build:mcpb` (full) / `build:mcpb:thin` from `globalise-mcp-server/` — each runs `npm run build`, stages a clean runtime tree under `mcpb-build/stage/`, and packs via `npx -y @anthropic-ai/mcpb` (no committed dep). **Full** ships the ~112 MB archival index in the bundle (30.4 MB `.mcpb`); **thin** omits it (4.7 MB) and the server downloads it on first `find_archival_documents` call (`ensureDatabaseFile()` in `database.ts`). Both stage `data/reference.sqlite` (the commodities + measures glossaries — small, so even thin ships it; without it those two tools degrade to "unavailable"). The manifest version is stamped from `package.json` at pack time. `npm run test:mcpb` stdio-smoke-tests the staged tree (auto-detects variant; thin run exercises the real first-run download). `.mcpb` output and the build are documented in `MCPB.md`. See [memory: mcpb-node24-baseline] for history.

## Conventions

- `offline/` is gitignored; ignore unless explicitly requested.
- Archived CHANGELOG (v1.0–v1.16) and completed/deferred TODOs are in `offline/outdated/`.
- **Do not modify `.gitignore` without explicit user approval.**
- "TODO" or "todo" means `globalise-mcp-server/TODO.md`.
- CLAUDE.md is tracked in git.
- A SessionEnd hook automatically captures `★ Insight` blocks to `offline/INSIGHTS.md`.

## Shell Best Practices (Lesson Learned)

Complex curl commands fail due to shell parsing. Write JSON to a temp file instead of inline `-d '{...}'`. If curl fails with parsing errors, restructure rather than retry.

## Common Errors

- **Vite UI build fails with `Rollup failed to resolve import "openseadragon"`:** the local `node_modules` is stale (predates a dep the committed `package-lock.json` already pins — e.g. an old tree still on vite 5 with no `openseadragon`). Run `npm install` (or `npm ci` for a clean slate) before `npm run build`; do **not** add the import to `rollupOptions.external` (the viewer bundle must inline it). The committed lockfile is correct; only the on-disk install drifted.

- **Editor flags `Cannot find name 'process'` (or other Node globals) in `scripts/*.ts`:** false positive — do **not** install deps or touch tsconfig. `tsconfig.json` `include` is `src/**/*` only, so `scripts/` is outside the TS project; the editor LSP falls back to an inferred project that doesn't load `@types/node`. The scripts run via `tsx` (transpile-only), so `npm run build` (tsc over `src/`) and `npm test` (tsx) both stay green. **Corollary:** type errors in `scripts/` are caught by neither the build nor the tests — they surface only at runtime, so review test-script types by hand (or run `npx tsc --noEmit` against the file with `@types/node` on the include path if you need a real check).

---

## Upstream API Reference

These endpoints are external to the codebase and useful when debugging or modifying the server.

| Component | URL |
|-----------|-----|
| Frontend SPA | `https://transcriptions.globalise.huygens.knaw.nl` |
| Search API | `https://gloccoli.tt.di.huc.knaw.nl` (Broccoli/Gloccoli) |
| Text Storage | `globalise.tt.di.huc.knaw.nl/textrepo` |
| Annotations | `annorepo.globalise.huygens.knaw.nl` |
| Images | `service.archief.nl/iip` (IIIF) |

### Key Endpoints

```
POST https://gloccoli.tt.di.huc.knaw.nl/projects/globalise/search
  Query params: indexName, fragmentSize, from, size, sortBy, sortOrder
  Body: { text: "query", terms: {}, aggs: {...} }

GET https://gloccoli.tt.di.huc.knaw.nl/projects/globalise/urn:globalise:{doc_id}
  Query params: overlapTypes, includeResults, views, relativeTo
```

---

## Deployment

| Branch | URL |
|--------|-----|
| `main` | `https://globalise-mcp-production.up.railway.app/mcp` |
| beta — now also tracks `main` (no active dev branch) | `https://globalise-mcp-beta-production.up.railway.app/mcp` |

Health check: `/health` — returns `{ status, name, version, node }`. The `node` field is the runtime version Railway actually selected; `curl <url>/health` to confirm it's `v24.x` after a deploy.

**Auto-deployment:** Railway deploys automatically when pushed to GitHub. Each service's GitHub source watches **one configured branch** (not a `feature/*` glob): production tracks `main`; beta tracks whatever branch is set as its source — **currently also `main`** (repointed 2026-06-13; see note below). Any push that moves the watched ref triggers a deploy, including a `git commit --amend` + push (the deploy fires on the ref update, not on a changed version/message). To confirm which commit is live when two commits share a version, check the deployment's commit SHA in the Railway dashboard; when versions differ, `/health` is enough. To repoint beta at a different branch, change the service source in the Railway dashboard.

**`worktree-p0-refactor` retired (done 2026-06-13):** the `worktree-p0-refactor` local branch + git worktree were retired on 2026-06-12 (fast-forward-merged into `main` at `7d6e09a` / v2.8.1); the remote `origin/worktree-p0-refactor` was kept temporarily as the Railway beta deploy source. On 2026-06-13 the beta service (`globalise-mcp-beta`, project `endearing-warmth`) was repointed to `kintopp/globalise-mcp@main` and the remote branch was deleted (`git push origin --delete worktree-p0-refactor` + local prune). Net: beta now tracks `main` (so beta == prod until a new dev branch is set as its source); no `worktree-p0-refactor` ref remains anywhere. To stand up a future beta dev branch, push it and repoint the beta service source in the Railway dashboard.

**Node version:** Railway's builder selects Node from `globalise-mcp-server/package.json` `engines.node` (`>=24.15.0 <25.0.0`) and `.nvmrc` (`24.16.0`) — Node 24, matching Claude Desktop's bundled runtime. The builder pins the major; the patch is its choice. The code runs on any Node 24.x (the `>=24.15` floor is for Claude Desktop parity, not a hard requirement — `node:sqlite` is flagless from 24.0). To force the major, set the `NIXPACKS_NODE_VERSION=24` service variable. Verify post-deploy via `/health`.

---

## Version Management

**Every code change requires a version bump.** This has been forgotten in the past.

Update ALL of these (they must match):

1. `package.json` — the only code location; `SERVER_VERSION` (and `/health`) read it at startup
2. `CLAUDE.md` — Current Version below
3. `CHANGELOG.md` — new entry with date
4. `package-lock.json` — run `npm install` (no args) after bumping `package.json` so its two `version` fields follow; don't hand-edit. It has silently lagged before (was stuck at 2.4.0 through v2.5.3).

### Current Version: 2.9.1 (v2.9.1 = **Railway start command changed from `npm start` to `node dist/index.js`** in `railway.json` (deploy-config only — no `src/`, DB, `.skill`, or behavior change to the server's request handling). Root cause surfaced by a scary-looking `npm error signal SIGTERM` in the **beta** deploy logs: beta has Railway **app-sleep** enabled (`Sleep when inactive: true`; prod does not), so after a few minutes idle Railway `SIGTERM`s the container — normal lifecycle, deployment state `SLEEPING`, not a crash. But the entrypoint was `npm start`, putting **npm as PID 1**; npm does **not** forward `SIGTERM` to its `node` child, so the v2.7.5 graceful-shutdown handler **never ran on Railway** (the `[SHUTDOWN] SIGTERM received` line never appeared in any deploy log — proof) and npm exited non-zero, emitting the `npm error signal SIGTERM` lines on every beta app-sleep **and every prod redeploy**. Making `node` the direct process (Railway's `sh -c` `exec`s a single trailing command, so `node` becomes PID 1 / signal recipient) means the handler now runs — logs `[SHUTDOWN]`, drains in-flight `/mcp` requests, exits 0 — and the npm error lines are gone. `railway.json` is shared by both services, so prod gets working drain-on-redeploy too; beta app-sleep left enabled (harmless, saves resources). Patch bump (deploy infra). v2.9.0 = **`.mcpb` (MCP Bundle / Desktop Extension) packaging brought onto `main`** from the retired `feature/mcpb` branch, refreshed for the current 7-tool server. MINOR bump — new build/packaging path + an additive, thin-bundle-only runtime code path; **no behavior change** to the running server in its Railway / full-bundle configs. **Grafted, not rebased:** the machinery is almost entirely additive, so the 6 new files (`manifest.json`, `manifest.thin.json`, `.mcpbignore`, `MCPB.md`, `scripts/build-mcpb.ts`, `scripts/test-mcpb-bundle.ts`) + 5 npm scripts (`build:mcpb`, `build:mcpb:thin`, `validate:mcpb[:thin]`, `test:mcpb`) + the appended `ensureDatabaseFile()` in `src/utils/database.ts` and a guard at the top of `findArchivalDocuments` were re-derived onto the evolved v2.8.1 `src/` rather than replaying the branch's stale v2.6.x version bumps; `feature/mcpb` retired afterwards. `build-mcpb.ts` stages a clean runtime tree (`dist/` + bundled glossaries + production-only `node_modules`) and packs it via `@anthropic-ai/mcpb` (no new runtime dep — invoked through `npx -y`); the manifest version is stamped from `package.json` at pack time so it never drifts. **Two variants:** the **full** bundle ships the ~112 MB finding-aid index in the `.mcpb` (on-device, no network); the **thin** bundle omits it (small download) and the server fetches it once, lazily, on first `globalise_find_archival_documents` call — `ensureDatabaseFile()` does an atomic temp-file download + gunzip + rename, configured by the `ARCHIVAL_DB_URL`/`ARCHIVAL_DB_PATH` env the thin manifest sets; the full bundle never triggers it. **Key fix vs the v2.6.x branch:** `build-mcpb.ts` predated the reference DB, so it now stages `data/reference.sqlite` in **both** variants (it's small — only the large archival index is thinned), else `globalise_lookup_commodity`/`_measure` would silently degrade to "unavailable" inside the bundle; a build-time prerequisite check fails if it's missing. Both manifests now declare all **7** tools (added the two reference-glossary tools the branch predated). `test:mcpb` is **not** in the `npm test` chain (it needs a staged bundle from `build:mcpb` first). v2.8.1 = **doc-only refinement of `globalise_lookup_measure`** (always-loaded tool description in `src/index.ts` + SKILL.md W&M section + `.skill` repackaged) — **no code-logic change**, no DB rebuild, no schema change. Driven by a live finding from driving the local stdio server with 7 test prompts: the conversion `context` field is routinely **commodity-qualified, not just place-qualified** (`"rijst, Batavia"`, `"goud, zilver, Mokka"`), and the same unit's ratio differs by good — so the commodity-specific value lives in the `context`, not in the (sparse) definition. The "NOT a unit converter" clause in `index.ts` and the SKILL.md context bullet now state this explicitly with the literal format, so a client LLM routes the commodity caveat through `context` rather than over-relying on the prose definition (which only ~22% of units carry). Patch bump mirroring the v2.7.2 commodities doc-refinement precedent. **Also grew `scripts/test-measures.ts` 18→30 assertions** (cases §9–§12, folded into this version since they lock the same refinement): §9 homonyms (`roede`→≥2 units by `type`), §10 `context` non-empty + commodity-varying (Bahar 14 distinct contexts) + bidirectional conversion retrieval (`from_unit OR to_unit`, via Frasila), §11 the 22 self-referential `1 X = 1 X` rows kept (editorial guard), §12 every result validates against `lookupMeasureOutputSchema` (schema-drift guard) + definitions are `nl`/`en`-only. Test-only; full `npm test` green. Deferred test gaps (need scaffolding): `from`-offset pagination, input-schema rejection, reopen/connection-state, cross-tool DB sharing, FTS-operator-through-tool, DB-unavailable path. v2.8.0 = **new tool `globalise_lookup_measure`** — flat-glossary lookup over the GLOBALISE VOC weights & measures dataset (v3.0; **213** units of weight/volume/length/area/quantities/misc, **385** period spelling variants, **731** period-reported conversion relationships; from the *Memoriën van Munten, Maaten, en Gewigten* 1764–1771; hdl:10622/MDNVH5, CC-BY-SA-4.0). Near-clone of the v2.7.0 commodities tool (plan 004): reuses the shared `data/reference.sqlite`, `createConnectionState`, the FTS5 sanitizer, and the registration ceremony — MINOR bump (new tool, no behavior change to existing tools). New `data/sources/weights-measures.json` (copied verbatim from `archived-resources/`). The reference-DB build script `build-commodities-db.ts` now builds the **whole** reference DB (commodities **+** measures — both tools share the single delete-and-rebuilt file, so a schema change to either rebuilds both): it gains `measures` + `measure_conversions` tables (+ from/to indexes) and a `measures_fts` FTS5 index (columns label/variants/def_text, the tool's bm25 weights `10,5,1`); committed `data/reference.sqlite.gz` regenerated; `build:db:commodities` npm name kept (rename to `build:db:reference` deferred — touches ensure-flow + docs). New `src/tools/measures.ts`: `query?` FTS over label+variants+(sparse) definition text, or omit to page alphabetically by label; per-result `label`, `type`, `variants[]`, `definitions[]` ({nl?,en?} only — no source key), and `conversions[]` (the ratios the unit appears on either side of, each with its `context`). Registered as the **7th** tool in `index.ts`; the always-loaded description frames it as **NOT a unit converter** (ratios are period claims that vary by settlement/commodity — read `context`; a self-referential "1 X = 1 X" attests use without a recorded local equivalence), notes `type` is load-bearing (roede/voet/ammonam homonyms), and points variants at `search_transcriptions` for recall; `unit_id` stays internal. New `scripts/test-measures.ts` (`test:measures`, 18 assertions, chained into `npm test` after `test:commodities`); smoke `EXPECTED_TOOLS` now 7. **Editorial calls (Editorial Decisions rule):** dataset shipped **as-is** — no filtering; the **22 self-referential conversions** (from===to) and the few exact-duplicate rows are KEPT (incomplete attestations, not errors); the only transforms are structural (`'; '`-joining spelling variants — the convention `commodities.alt_labels` uses, which echoes the lowercased label among its own variants, KEPT — and JSON-serializing the per-unit definitions array). Definitions are sparse (~22% of units, English ~6%) so tool + SKILL lead with label+type+variants+ratios, definitions a bonus. Build-time integrity gates **fail** (not warn): every lookup variant + every conversion endpoint must resolve to a unit, and parsed 213/385/731 must equal `_meta`. SKILL.md's "Weights & measures (not a tool here)" section rewritten **in place** to introduce the tool (keeping its unstable-units caveat) + the "six tools"→"seven tools" overview row; `.skill` repackaged (503 lines); TODO.md W&M item marked Done and the conflicting "Remove Unnecessary Translations" item flagged as a now-decision-point (v2.8.0 shipped EN intact). No change to existing tools/schemas/viewer. Full suite green (10 sub-suites incl. `Measures lookup passed.`). v2.7.16 = **39 more commodities AAT wrong-sense fixes (Tier 1+2) + provenance retrofit** (data-only, continues v2.7.14–15). Tier 1 = 3 definition-misassignment bugs (`kruiden` def was "Wollen stoffen", `koekenpan` a textile, `vorm` "Gujarat cloth"). Tier 2 = 36 homonyms re-grounded on the GLOBALISE corpus (`comptoir` home-office→trading post, `mutsje` cap→liquid measure, `Tol` toy→toll, `harp` instrument→pepper-sieve, `kiel` smock→keel, `gewas` plant-kingdom→crop, `Pokhout` hackia→lignum vitae, `roodkoper` tombac→pure copper, `knie` furniture-leg→ship's-knee, etc.). All 39: `definitionSource: editorial`, `definitionLanguage: nl`, `web_verification: verified`, corpus-cited desc, Dutch def with an NB-disambiguator. Provenance retrofit of the earlier 10: added `web_verification: verified` (TSV-only flag, not in the DB/tool output — the build doesn't read that column) and fixed `fanum`'s `definitionSource_url` to the deterministic VOC-Glossarium deep-link. **Held back 9** where the AAT sense is actually defensible (`pees`/`japon`/`tabijn`/`dril`/`pers`/`klink`/`broodmes`/`handgreep`/`Ophaler`) — flagged, not corrected. Tier 3 (proper-noun noise) + Tier 4 (ambiguous) untouched. total 3,508 unchanged; aat rows 429→380; commodities.tsv + regenerated reference.sqlite.gz; test:commodities green. Full audit in plans/aat-wrong-sense-findings.md (untracked). No src/schema/.skill change. v2.7.15 = **more commodities AAT wrong-sense fixes** (data-only, continues v2.7.14): corrected five further `definitionSource: aat` rows whose definitions carried the wrong word-sense — `lei` (Chinese ritual bronze → slate), `ledikant` (children's crib → adult bedstead), `soldeerbout` (plumbing grozing-iron → soldering iron), `chamois` (chamois leather → tan/buff textile colour), `priem` (textile bodkin → cannon laad-/maarlpriem awl). Re-grounded against the GLOBALISE corpus; `definitionLanguage: nl`, `definitionSource: editorial`, confidence high for `ledikant` else medium. `commodities.tsv` + regenerated `data/reference.sqlite.gz`; total 3,508 unchanged; test:commodities green. These came from corpus-checking the remaining ~396 `aat` rows; the parallel sweep is INCOMPLETE — an account session-limit killed 11 of 12 agent batches before they reported, so only batch 04 (33 terms) + the lei/beeldje/pop deep-dive landed (`beeldje`/`pop` re-examined and KEPT as real figurine/doll trade goods; `peer` confirmed correct). ~330 `aat` rows (batches 01–03, 05–12) still unchecked; batch files /tmp/aat_b*.json retained for re-run. No `src/`/schema/`.skill` change. v2.7.14 = **commodities AAT wrong-sense fixes** (data-only, Editorial Decisions rule): corrected five `definitionSource: aat` rows whose definitions carried the wrong word-sense — `fanum` (Gallo-Roman temple → small South-Indian coin), `manufacturen` (manufactories/organisations → manufactured textile piece-goods), `staalwerk` (steel-frame construction → steelware), `muurplaat` (modern wallboard → carpentry wall-plate), `bloedsteen` (green-chalcedony bloodstone → hematite). Root cause: the AAT enrichment string-matched each Dutch lemma to a Getty art/architecture concept and imported its scope note at `confidence: high` with no sense check (`confidence` measures string-match-to-authority, not sense-correctness — all 429 `aat` rows are `high`). Senses re-grounded against the GLOBALISE transcription corpus (+ VOC-Glossarium for `fanum`, which the live thesaurus left undefined/NOT-YET-CLASSIFIED). Touched `data/sources/commodities.tsv` + regenerated committed `data/reference.sqlite.gz`; total **3,508** unchanged; `prefLabel_nl`/UUIDs untouched; corrected rows set `definitionLanguage: nl`, `definitionSource: vocGlossarium|editorial`, `confidence` medium (high for the strongly-attested `manufacturen`/`muurplaat`); `npm run test:commodities` green. Found via a sweep of all 429 `aat` rows (34 art/architecture/anachronism-keyword-flagged → corpus-checked); `foelie (metaal)` confirmed CORRECT (deliberate rare second sense, mace is its own entry); `beeldje`/`lei`/`pop` still under review; other ~395 `aat` rows not yet corpus-checked. No `src/`/schema/`.skill` change. v2.7.13 = **type-check the document-viewer app in `npm test`** (plans/003, retires the deferred residual of code-review finding 10 / the TODO.md "Type-check the Document Viewer" item). Build-infra only — no `src/` change, no viewer-source change, no DB rebuild, no `.skill`. The viewer (`apps/document-viewer/src/viewer.ts` + `parse-result.ts`) was **excluded from tsc** (root `tsconfig.json` `exclude: ["apps"]`) and only vite-transpiled, so a type error *inside* viewer code — e.g. using a renamed server-contract field — compiled green and surfaced only at runtime in the iframe. New `apps/document-viewer/tsconfig.json` (`strict`, `target ES2022`, `module ESNext`, `moduleResolution bundler`, `lib [ES2022,DOM,DOM.Iterable]`, `types [openseadragon,node]`, `noEmit`, `skipLibCheck`) + `test:viewer-typecheck` (`tsc -p apps/document-viewer`) chained into `npm test` immediately before `test:viewer-build` (type errors fail faster than a vite build). Two deliberate divergences from the TODO sketch, both empirically grounded: **`moduleResolution: bundler`** not the sketched `NodeNext` (bundler is what verified clean and matches vite's resolution of the `.js`-suffixed cross-boundary `import type` from `src/tools/document-viewer.ts`), and the anticipated **"first-pass cleanup" of OSD/ext-apps/DOM type errors did not happen** — viewer sources type-checked clean on the first run, zero errors (`openseadragon` ships its own types, so no `@types/` package needed). Root `tsconfig.json` left untouched (the two compilations have incompatible lib/types needs — the viewer needs DOM, the server must not see it); server `tsc --noEmit` + `npm run build` (vite) both confirmed unaffected. Negative test confirmed the gate bites (deliberate `const x: number = 'oops'` in viewer.ts failed the suite, then reverted). Deferred still: typechecking `vite.config.ts` + `scripts/*.ts` (both transpile-only via vite/tsx). v2.7.12 = **first tests for the network-backed tools + the HTTP shutdown path** (plans/002). Tests-only — no `src/` change, no DB rebuild, no `.skill`. New `scripts/test-live-api.ts` (`npm run test:live`, **opt-in, network-required, NOT in the `npm test` chain** — this repo's `npm test` is offline-capable by design) exercises the three previously-untested tools `search_transcriptions`/`retrieve_document`/`navigate`, validating each live response against the tool's own **output** zod schema (the upstream-contract-drift detector) plus targeted assertions: 6 cases / 16 checks against the stable doc `NL-HaNA_1.04.02_9966_0106` (basic+inventory-filtered+matchAll search, retrieve, next-page navigate→`0107`, URN round-trip). One plan assertion was corrected against the live contract: search result `id` is the **`urn:globalise:NL-HaNA_…` URN form** (uniform with `retrieve`'s id), not the bare doc-id the plan guessed — schema parse stayed green, so this was a wrong expectation, not drift. New `scripts/test-http-shutdown.ts` (`npm run test:http-shutdown`, chained into `npm test` after `test:smoke` — both need `dist/`) spawns the built server in HTTP mode on port 3917, polls `/health`, sends SIGTERM, asserts clean **exit 0** (was 143 pre-v2.7.5) + the `[SHUTDOWN] SIGTERM received` log; verified non-flaky across two consecutive runs. README "Development" block documents `test:live` as the opt-in network suite. v2.7.11 = **HTTP-edge hardening + version-sync enforcement** (plans/001). Four small batched fixes, no DB rebuild, no `.skill`. (1) The per-request `res.on('close')` teardown in `http-server.ts` floated two promises — `transport.close()` + `server.close()` (the per-request `McpServer`, not the outer `http.Server`) — so a rare close-time rejection became an unhandledRejection (fatal on Node 24), killing the server for every client; both are now `.catch(()=>{})`'d fire-and-forget. (2) `/health` now passes through `originGuard` like the three `/mcp` routes (MCP-spec MUST / DNS-rebinding mitigation): no-Origin requests (curl, Railway health checks) still pass, but a rebinding page can no longer read name/version/node — same guard instance, no extra startup log. (3) The stale CHANGELOG deployment header (claimed prod v1.23.0 / beta v1.24.1 while the package is 2.7.x) replaced with a version-agnostic pointer (deploys verifiable via `GET /health`). (4) New `scripts/check-version-sync.ts` (`npm run test:version`, prepended to the `npm test` chain — cheapest first) asserts all four version locations agree (package.json, package-lock.json root + `packages[""]`, CLAUDE.md Current Version, CHANGELOG newest entry); the ritual silently drifted before (lockfile stuck at 2.4.0 through v2.5.3), now it can't. tsc clean; curl matrix 200/403/200 on `/health`. v2.7.10 = **commodity → thesaurus permalink**. `lookup_commodity` now returns a `thesaurusUrl` on every result — the stable handle permalink `https://hdl.handle.net/20.500.14722/thesaurus:commodities:<UUID>` into the public GLOBALISE Commodities thesaurus (Skosmos), exposing the curated SKOS hierarchy (broader/narrower) + the concept's cited source (often a Zotero record) — the taxonomy/sources the flat tool deliberately omits, WITHOUT re-importing the unreliable `skos:broader` tree. **Read-path only, no DB rebuild**: the `uuid` column already shipped in the committed reference DB (the build script stored the full PoolParty URI as an internal key), so `thesaurusUrl` is derived in JS in `mapRow` (`buildThesaurusUrl`: trailing UUID of the stored key → `UUID_RE` guard → handle prefix, else null); committed `data/*.sqlite{,.gz}` byte-identical, search/ranking unchanged (output schema gains one nullable field; strict-schema + smoke tests green). Premise discovered live: the vocabulary migrated off PoolParty (`digitaalerfgoed.poolparty.biz/globalise/<UUID>`) to a handle-based Skosmos home (`data.globalise.huygens.knaw.nl/hdl:20.500.14722/thesaurus:commodities:<UUID>`) but **preserved every UUID verbatim** (the datasheet promised this), so the key v2.7.0 stored as "internal, never surfaced, not publicly resolvable" is now publicly resolvable via the handle (302→Skosmos page); the now-false comments in `commodities.ts`/`build-commodities-db.ts` were refreshed + a SKILL.md "Looking up commodities" note added + the **always-loaded tool description in `index.ts`** updated to name `thesaurusUrl`/when-to-offer and fix its stale "Concept IDs are internal and not returned" clause (the tool description is the dependable surfacing channel — an output-field `.describe()` alone doesn't reliably reach the client LLM, and non-skill clients never load SKILL.md). **Validated end-to-end** before shipping: swept the live Skosmos REST index (nl∪en buckets) → all **3,508** of our UUIDs resolve to a live concept page, **0 dead links** (live scheme 3,621 concepts; ours a clean subset). v2.7.9 = **build-script dedup** (code-review **finding 19**), tooling only — no server/DB/artifact change. New `scripts/db-build-utils.ts` holds the forked helpers: `runInTransaction` (was identical in both build scripts), `writeGzipArtifact(dbPath)` (the `createGzip(9)` artifact tail), and a parameterized `ensureDb(opts)` (present→`*_DB_URL` download→committed `.gz` gunzip→source rebuild→warn, with crash-safe temp-write). Both build scripts import the first two; both ensure scripts are now thin wrappers passing paths/label/env-var names. ensure-reference-db.ts gains the `REFERENCE_DB_URL`/`_TOKEN` download branch + temp-write its hand-fork had dropped, and its stale comment (`npm run ensure:db:commodities`, which doesn't exist) is fixed to `npm run ensure:db`. Scripts run via tsx (not in the tsc project), so verified by RUNNING: build:db:commodities + ensure:db against a temp REFERENCE_DB_PATH (build→3,508 rows+gz; ensure→decompressed committed .gz), committed data/*.sqlite{,.gz} untouched; suite 150. **This completes the actionable code-review findings (1–20); only deferred/won't-do items remain — see CODE-REVIEW-FINDINGS.md (#7 per-host throttle = deferred-until-concurrency; ApiError-class migration = deferred; apps/ viewer typecheck = TODO).** v2.7.8 = **P4 cleanup pass** (code-review **findings 11, 12, 13, 14, 16, 17, 18, 20-index/viewer**). Mostly internal hardening + dead-code removal; suite 150 assertions. (11) empty/whitespace settlement & chamber normalized to undefined once at top of findArchivalDocuments (param renamed rawInput) — `{settlement:''}` no longer skips GM/returns all OBP, `{source:'gm',settlement:''}` no longer throws. (12) sanitizeFtsQuery's 3 bare catches now rethrow non-query errors (finalized handle/BUSY/IO) instead of masking as "Invalid full-text query"; discriminator `isFtsQueryError` = node:sqlite `code==='ERR_SQLITE_ERROR' && errcode===1` (empirically confirmed: fts5 syntax / unterminated / "no such column" are errcode 1; finalized stmt is ERR_INVALID_STATE). (13) getCachedApiGet: `!==undefined` hit test + per-cache WeakMap in-flight dedup. (14) getDatabase/getReferenceDatabase configure a local conn and publish to the module var only after pragmas succeed (no half-configured-handle flip-flop). (16) FTS contract single-sourced: `FTS_OPERATORS`+`FTS_AUTOQUOTE` exported from fts.ts, interpolated into both describes + both index.ts registrations (SKILL.md stays hand-maintained). (17) document-id URN strip now case-insensitive + anchored (`/^urn:globalise:/i`). (18) removed dead code: configCache, LRUCache.clear/size/has/delete (has() mutated LRU), getDocument's always-true includeAnnotations option, the inert searchInputSchema zod (→ plain interface, dropped never-passed indexName/languageLabels), viewer parse-result's unreachable 3rd fallback. (20-index) extracted `outputSchemaField()` (R8 gate) + `runTool()` (try/catch→errorResponse) shared across registrations; `formatError` hardened to surface `{message}` objects not `[object Object]` (full ApiError-class migration DEFERRED → TODO). (20-viewer) one combined highlight regex per render (was new RegExp/term/line); escapeHtml regex-based (no per-call div, now also escapes quotes for title="" safety); splitter mousemove/mouseup registered once at module scope (were per-render, leaked) reading bounds once on mousedown (no per-move reflow). **Finding 19 (build-script dedup) split into v2.7.9.** v2.7.7 = **document-viewer fixes** (P3 viewer-only batch, code-review **findings 8, 9, 10**). Viewer-only (`apps/document-viewer`), no server-tool behavior change, no DB rebuild, no `.skill`. (8) Rotation desync: `rotateImage` tracked angle in a module-level `currentRotation` never reset when a new doc loaded a fresh OSD viewer at 0° → first rotate jumped to 180°. Now derives from live `viewer.viewport.getRotation()` (counter deleted, resetView no longer touches it). (9) Error screens dumped the raw JSON envelope: server `errorResponse` emits `JSON.stringify({error,suggestion,tool})` as the text and the viewer rendered it verbatim. isError branch now JSON-parses it → shows `error` as message + `suggestion` as a `.error-suggestion` line, falls back to raw text for non-JSON. (10) Contract typed twice, never cross-checked: viewer had hand-written `DocumentData`/`ArchivalContext` duplicating the server's `viewDocumentUiOutputSchema` (zod) across two compilations (tsc src/ vs vite apps/) → a server rename failed only at runtime. Viewer now `import type`s the server's zod-inferred `ViewDocumentUiOutput`/`ArchivalContext` (erased by esbuild — zod stays out of the bundle, size unchanged); parse logic extracted to browser-free `apps/document-viewer/src/parse-result.ts`; new `scripts/test-viewer-protocol.ts` (npm test `test:viewer-protocol`) feeds a schema-validated payload through it, catching id-detection/schema drift `test:viewer-build` couldn't. **Deferred:** apps/ still excluded from tsc (vite transpile-only), so a type error in viewer.ts itself is caught by neither build nor tests — only the extracted parse path is node-testable; full viewer type-checking (apps tsconfig + DOM/OSD/ext-apps types) deferred. Suite now 147 assertions / 6 suites. v2.7.6 = per-connection **statement-cache refactor** across the two SQLite tools (code-review **findings 6, 15, 20-commodities**). **No behavior change** — same I/O + result contract; full suite green (142 assertions, +2). New `createConnectionState(init)` in `src/utils/database.ts` replaces the hand-copied `staticState`/`getStaticDbState` (`staticState?.db !== db` handle-keying + FTS probe + cached COUNT) in both archival-index.ts and commodities.ts (a 3rd copy was planned for weights-&-measures). The factory owns the **handle-keying invariant** (a prepared stmt belongs to its handle → cache rebuilt after closeDatabase()+reopen) and exposes a per-connection `prepare(sql)` that caches statements by SQL string; each factory call owns its own slot so the archival + reference DBs never collide. archival-index.ts: all per-call statements (OBP/GM COUNT+SELECT, 3 aggregation GROUP BYs — up to 8 inline prepares with a query + source:'all') now go through cached `prepare()`; lazy unfiltered-agg result cache unchanged. commodities.ts: FTS COUNT + both SELECT shapes cached too. New test-archival-index.ts §7 locks reopen (query → closeDatabase → same query → identical total/aggregations). **Deferred** (finding 6 option-b): materializing FTS-matched rowids once per call (temp table/CTE) to avoid re-EXECUTING the MATCH 8× — conflicts with the statement cache (a per-call temp table invalidates cached stmts referencing it) and the MATCH cost is modest on the warm read-only DB; only the re-prepare cost was the primary fix. Finding 20's other items (viewer regex/handlers, STRUCTURED_CONTENT gate, formatError) remain open. v2.7.5 = graceful HTTP shutdown (P2 **finding 5**). Every Railway redeploy SIGTERMs the process, but `shutdown()` was `closeDatabase(); process.exit(0)` — cutting in-flight `/mcp` requests mid-response and accepting new connections right up to exit. Root cause: `createHttpServer` returned the express `app` and **discarded** the `http.Server` from `app.listen()`, so nothing could `.close()` it. Fix: http-server.ts captures + returns the `http.Server` (typed `: Server`); index.ts holds it in a module-scope `httpServer` (http mode only) and `shutdown()` drains it — `closeIdleConnections()` → `server.close()` (stop accepting, await in-flight) → `closeDatabase()` + exit, with a 10s `unref`'d `SHUTDOWN_TIMEOUT_MS` backstop so it never hangs past Railway's grace window. Stdio mode unchanged (synchronous exit). Verified live: SIGTERM → exit **0** (was 143) with the `[SHUTDOWN]` sequence logged, backstop not triggered. No DB rebuild. v2.7.4 = fixed the four **P1 findings** from the v2.7.3 whole-codebase review (`CODE-REVIEW-FINDINGS.md`), batched into one version per that file's process notes. No DB rebuild, no `.skill` change. (1) `retrieve_document`/`navigate` no longer hard-error when an upstream page lacks a license: `license` was mapped unconditionally from `metadata.comment`, but both output schemas declared it a **required** string and `STRUCTURED_CONTENT` (default-on) validates `structuredContent` against the schema — a comment-less page → `license:undefined` → zod failure → whole call errored. Now `.optional()` in `getDocumentOutputSchema` (document.ts) + `navigateOutputSchema` (convenience.ts), `PageMetadata.comment` optional in types.ts, and a shared `normalizeLicense()` (document.ts, reused by document-viewer.ts) guards the missing case **and** strips the `"license: "` prefix — retrieve/navigate now return the bare value (`"CC-BY-4.0"`), matching the viewer. (2) Folio filters no longer silently dropped: `folioFrom`/`folioTo` sat inside `if(inventoryNumber)`, so a folio range without an inventory was discarded and all rows returned as if filtered. Folio now throws a `ToolError` without an `inventoryNumber`, and is treated as **OBP-only like settlement** (chosen via AskUserQuestion over "apply to GM too"/"keep+note", per Editorial rule) — `source:"all"`+folio skips GM with a note, `source:"gm"`+folio throws; kills the old mixed folio-filtered-OBP + unfiltered-GM response. `hasObpOnlyFilters` now includes folio; incompatible/skip messages generalized. buildObp/GmWhereClause unchanged (the top-of-fn guard guarantees folio⇒inventory). (3) matchAll aggregations described the wrong population: matchAll filters upstream on ONE language (ES computes aggs over that pool) then post-filters results/total to ALL-languages pages — aggregations described e.g. all Malay hits, unflagged. Now recomputed client-side over the matched set (`aggregateResults()` in search.ts), upstream aggs no longer requested in matchAll mode (`includeAggregations:!useMatchAll`), note updated. (4) `find_archival_documents` registration in index.ts still documented the pre-v2.7.3 "phrase-escape retry sets a note" contract, contradicting the schema describe's auto-quote contract — rewritten to match (doc-only). Findings 5-20 remain open in CODE-REVIEW-FINDINGS.md. v2.7.3 = FTS5 sanitizer now **preserves boolean operators** instead of silently dropping them. The syntax error from hyphens/slashes/apostrophes is a FTS5 *query-parser* fault (barewords allow only letters/digits/underscore/non-ASCII), NOT a tokenizer issue — a different tokenizer is the wrong layer and would force a DB rebuild for no gain. New `escapeFtsTerms()` in `src/utils/fts.ts` quotes only the offending barewords while keeping `()`, `"phrases"`, `AND`/`OR`/`NOT`/`NEAR`, and trailing `*` intact (peeled outside the quotes); `sanitizeFtsQuery()` chain is raw → per-term → whole-phrase wrap → throw. Measured on the live 227K OBP index: lone punctuated terms were already fine via whole-wrap, but punctuated-term-plus-operator misfired to a silent 0 (`peper OR oost-indie` 0→984; `oost-indie*` 3→16). Query-side only, **no DB rebuild**; shared by `find_archival_documents` + `lookup_commodity`. Both tool descriptions rewritten; new `scripts/test-fts.ts` (24 pure-function checks, wired into `npm test` as `test:fts`) + strengthened archival §3. SKILL.md "Trap 2" rewritten to the auto-quote contract + `.skill` repackaged (rebuild command now documented above). Known FTS5 limit: implicit-AND before a `(group)` (`compagnie (a OR b)`) is rejected by FTS5 itself — write the explicit `AND`; such input falls back safely. v2.7.2 = doc-only refinement of `globalise_lookup_commodity` (tool description in `src/index.ts` + `SKILL.md`) after evaluating 8 test prompts across Sonnet/Opus/Fable — **no code-logic change**. Driven by one data fact: `altLabels` (period spelling variants) exist for only **~9.6%** of the 3,508 concepts (pepper/coffee/nutmeg have none), so the old "main use = query expansion via altLabels" framing oversold an absent feature. Reframe: core value = bilingual label resolution + sourced definition; recall engine = period-spelling reconstruction + fuzzy/wildcards (`koffie`→`coffij`); altLabels a bonus (rich for silk `zijde`=23, null for most). Also: keep source/confidence visible incl. in lists, say only what the definition states, prefer the definition over a sometimes-mistranslated `prefLabelEn` (e.g. `raapfoelie`≠"rapeseed oil"), expand source codes (WNT/AAT/vocGlossarium/PoolParty) for readers. Recall numbers (`koffie` 119 vs `coffij` 25,124) are live-confirmed and kept; source `.trig` checked — no extra SKOS labels, so this is prose not data. `.skill` repackaged. v2.7.1 = internal `/simplify` cleanup of the v2.7.0 commodities tool, **no behavior change** — full tests + smoke green: (a) the duplicated FTS5 `sanitizeFtsQuery` (probe-then-phrase-escape-with-`note`) extracted to shared `src/utils/fts.ts`, taking a prepared probe statement so each tool keeps its own table + caching; both `archival-index.ts` and `commodities.ts` call it; (b) `lookupCommodity` got a per-connection static-state cache (`commoditiesTotal` + `ftsProbe`, keyed by db handle) mirroring archival's `getStaticDbState`, dropping a constant `COUNT(*)` + probe re-prepare per call and a redundant second COUNT on the no-query path; (c) `notes[]` → single `note`, `ftsQuery` indirection folded into one `input.query` branch. v2.7.0 = new tool `globalise_lookup_commodity` — flat-glossary lookup over the VOC commodities thesaurus, 3,508 trade goods/concepts with bilingual NL/EN labels, period spelling variants, and a sourced+confidence-rated definition per concept. Reimplements the v1.17.0-removed `globalise://reference/commodities` MCP resource as the SQLite-backed tool the TODO planned, enriched well beyond the archived minimal thesaurus (labels+hierarchy only) with definitions+provenance. Primary use = query expansion: returned `altLabels` feed back into `search_transcriptions` (which is spelling-blind). New `data/reference.sqlite` (small reference-vocab DB, separate from the archival index; committed `.gz` decompressed at build via the extended `ensure:db`; built by `build:db:commodities` from committed `data/sources/commodities.tsv`). FTS5 over labels+variants+definitions, bm25-weighted so label/variant hits outrank definition-body hits; omit `query` to page alphabetically; reuses the archival tool's phrase-escape-with-`note` recovery. New DB-layer accessors (`getReferenceDatabase` etc.) in `src/utils/database.ts`. Editorial calls (Editorial Decisions rule): (1) glossary ships FLAT — both candidate classifications dropped as too unreliable to surface: the SKOS `skos:broader` tree is hollow ("NOT YET CLASSIFIED" holds 65% of concepts as direct children, rest is abstract SITC), and the LLM `class` misclassifies (e.g. "fire engine"→textile); a misleading taxonomy is worse than none. Abandoned `class`/`class_Source` columns also stripped from the committed `commodities.tsv` (full original stays in gitignored `offline/`). (2) `definitionSource`+`confidence` surfaced on every result since >half are LLM-generated and ~31% low/medium-low; (3) PoolParty concept UUIDs kept internal, never returned. Source: VOC Commodities Thesaurus, GLOBALISE/Huygens, CC-BY-SA-4.0, hdl:10622/YAWDOV)

---

## Rules

### Editorial Decisions on Content

**Flag content modifications for user review.** Any editorial decisions that modify, truncate, filter, or transform source data must be flagged before implementation and documented in CHANGELOG.md.
