{
  "$schema": "https://raw.githubusercontent.com/anthropics/mcpb/main/dist/mcpb-manifest.schema.json",
  "manifest_version": "0.3",
  "name": "globalise-voc-transcriptions-thin",
  "display_name": "GLOBALISE VOC Transcriptions (thin)",
  "version": "2.6.1",
  "description": "Thin variant: downloads the ~26 MB finding-aid index on first use instead of bundling it. Small install, one-time fetch.",
  "long_description": "Same ten tools as the full GLOBALISE bundle, but **without** the ~112 MB finding-aid index baked in — so the download is a few MB instead of ~31 MB. (The small commodities and weights & measures glossaries are still bundled.)\n\nThe first time you use **globalise_find_archival_documents**, the server downloads the compressed index (~26 MB `.gz`) from the configured URL, decompresses it into your data directory, and opens it read-only. Every later call reuses the cached copy; the download happens once. Startup and the other six tools (search, retrieve, navigate, view, lookup_commodity, lookup_measure) never wait on it — they call the public GLOBALISE / Nationaal Archief services, or the bundled glossaries, directly.\n\nConfigure the **index download URL** and **data directory** at install time. The default URL points at a local HTTP server (`127.0.0.1:8000`) for testing; for real use, point it at any HTTPS URL that serves `archival-index.sqlite.gz`. No account or credentials are required.",
  "author": {
    "name": "Arno Bosse",
    "url": "https://github.com/kintopp"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kintopp/globalise-mcp"
  },
  "homepage": "https://transcriptions.globalise.huygens.knaw.nl",
  "documentation": "https://github.com/kintopp/globalise-mcp/blob/main/globalise-mcp-server/MCPB.md",
  "support": "https://github.com/kintopp/globalise-mcp/issues",
  "license": "MIT",
  "keywords": [
    "globalise",
    "voc",
    "dutch-east-india-company",
    "transcriptions",
    "htr",
    "history",
    "archives",
    "nationaal-archief",
    "early-modern"
  ],
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/index.js"
      ],
      "env": {
        "TRANSPORT": "stdio",
        "ARCHIVAL_DB_PATH": "${user_config.data_directory}/archival-index.sqlite",
        "ARCHIVAL_DB_URL": "${user_config.archival_db_url}",
        "STRUCTURED_CONTENT": "${user_config.structured_content}",
        "DEBUG": "${user_config.debug_logging}"
      }
    }
  },
  "tools": [
    {
      "name": "globalise_search_transcriptions",
      "description": "Full-text search across ~4.8M transcribed VOC pages, with inventory and language filters and aggregations."
    },
    {
      "name": "globalise_retrieve_document",
      "description": "Retrieve one page by document ID or URN: full transcription, metadata, and prev/next page links."
    },
    {
      "name": "globalise_navigate",
      "description": "Fetch the previous or next page relative to a document ID, for sequential reading."
    },
    {
      "name": "globalise_find_archival_documents",
      "description": "Query a local index of 228K+ VOC finding-aid descriptions. Downloads the index once on first use (thin variant)."
    },
    {
      "name": "globalise_view_document_ui",
      "description": "Open an interactive viewer (IIIF scan image plus transcription, side-by-side) as an MCP App."
    },
    {
      "name": "globalise_inspect_page_image",
      "description": "Fetch a page scan or a region of it as an image the assistant can read directly (base64 crop via IIIF)."
    },
    {
      "name": "globalise_navigate_viewer",
      "description": "Steer an open viewer: zoom to a region or draw labelled overlay boxes onto the scan (append-only; needs the viewUUID)."
    },
    {
      "name": "globalise_poll_viewer_commands",
      "description": "Internal: the viewer iframe's command-polling channel (app-only, hidden from the agent tool list)."
    },
    {
      "name": "globalise_lookup_commodity",
      "description": "Look up VOC trade goods in the bundled commodities thesaurus (3,508 concepts): bilingual NL/EN labels, period spelling variants, and a sourced definition."
    },
    {
      "name": "globalise_lookup_measure",
      "description": "Look up VOC weights & measures in the bundled glossary (213 units): type, period spelling variants, definitions, and period-reported conversion ratios."
    }
  ],
  "tools_generated": false,
  "prompts_generated": false,
  "user_config": {
    "archival_db_url": {
      "type": "string",
      "title": "Index download URL",
      "description": "URL the finding-aid index is fetched from on first use. Must serve archival-index.sqlite.gz (gzip) or archival-index.sqlite. Default targets a local test server (run: python3 -m http.server 8000 --directory <repo>/globalise-mcp-server/data).",
      "default": "http://127.0.0.1:8000/archival-index.sqlite.gz",
      "required": false
    },
    "data_directory": {
      "type": "string",
      "title": "Data directory",
      "description": "Writable folder where the downloaded index is cached. Created if it does not exist.",
      "default": "${HOME}/.globalise-mcp",
      "required": false
    },
    "debug_logging": {
      "type": "boolean",
      "title": "Debug logging",
      "description": "Write per-tool diagnostic lines (including the download progress) to the server's stderr log.",
      "default": false,
      "required": false
    },
    "structured_content": {
      "type": "boolean",
      "title": "Send structured tool output",
      "description": "Include machine-readable structuredContent alongside text results. Keep enabled for Claude Desktop; disable only for hosts that reject structured output.",
      "default": true,
      "required": false
    }
  },
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=24.0.0"
    }
  },
  "privacy_policies": []
}
